#include <stdio.h>
/**
 * _isupper - checks for upper and lowercase letters
 * Return: 0 if upper 1, 1 if lower
 */

int _isupper(int c)
{
int i = 0;

if (i >= 'A' && i <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
