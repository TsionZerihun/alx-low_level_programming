#include <stdio.h>
/**
 * _isdigit - checks if its digit from 0 to 9
 * @c: takes in a digit
 * Return: 1 if its a digit, otherwise 0
 */

int _isdigit(int c)
{
if (c <= 0 && c >= 9)
{
return (1);
}
else
{
return (0)
}
}
