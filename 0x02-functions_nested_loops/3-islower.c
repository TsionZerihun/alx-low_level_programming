#include "main.h"
/**
 * -islower - return 1 if lowecase and o otherwise
 *  @c: takes in  a character
 *  Return: o for upper, 1 for lower
 */
int _lower(int c)
{
if (c >= 'a' && c <= 'z')
{
return(1);
}
else
return(0);
}
}
